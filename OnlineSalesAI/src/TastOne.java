
public class TastOne {
	/*
	 * SELECT d.NAME AS DEPT_NAME, AVG(s.AMT) AS AVG_MONTHLY_SALARY FROM
	 * salary_table s JOIN employee_table e ON s.EMP_ID = e.ID JOIN department_table
	 * d ON e.DEPT_ID = d.ID GROUP BY d.NAME ORDER BY AVG_MONTHLY_SALARY DESC LIMIT
	 * 3;
	 */
	
	
}
